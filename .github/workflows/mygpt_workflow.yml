name: My Workflow
on:
  push:
    branches:
      - main

jobs: 
  build:
    runs-on: unbuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v2

      - name: Run a script
        run: |
          echo "hello, Emma!"
         
